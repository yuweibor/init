import { Typography } from 'antd'
import * as React from 'react'

export default () => {
    return (
        <div style={{ padding: '20%' }}>
            <Typography>数据流</Typography>
        </div>
    )
}
